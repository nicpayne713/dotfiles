## Setup

```
ln -s .zshrc ~/.zshrc
ln -s .vimrc ~/.vimrc
ln -s .tmux.conf.local ~/.tmux.conf.local
ln -s .config/alacritty/alacritty.yml ~/.config/alacritty/alacritty.yml
ln -s .scripts/ta /usr/local/bin/ta 
```
